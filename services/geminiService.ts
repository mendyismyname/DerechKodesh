
/**
 * AI services have been disabled for this build.
 */
export const explainConcept = async (conceptName: string): Promise<string> => {
  return "AI explanation is disabled.";
};

export const analyzeTextStructure = async (hebrewText: string): Promise<null> => {
    return null;
};

export const generateSugyaDeepData = async (title: string, mainText: string) => {
    return null;
}
